# spring
Spring Learning

**git commands to fix .gitignore**

- Removing all the files from repo
```
git rm -r --cached .
```
- Add the files again
```
git add .
```
- commit your changes
```
git commit -m ".fixing gitignore"
```
